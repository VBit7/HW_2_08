Install your driver
Run the following on the command line
Note: Use appropriate Python 3 executable
python -m pip install "pymongo[srv]"


Add your connection string into your application code
mongodb+srv://user:<password>@cluster8.w3d66gu.mongodb.net/?retryWrites=true&w=majority&appName=Cluster8



from pymongo.mongo_client import MongoClient
from pymongo.server_api import ServerApi

uri = "mongodb+srv://user:<password>@cluster8.w3d66gu.mongodb.net/?retryWrites=true&w=majority&appName=Cluster8"

# Create a new client and connect to the server
client = MongoClient(uri, server_api=ServerApi('1'))

# Send a ping to confirm a successful connection
try:
    client.admin.command('ping')
    print("Pinged your deployment. You successfully connected to MongoDB!")
except Exception as e:
    print(e)